# 线索转化逻辑分析

## 🔍 当前实现

### LeadService.convertLead (L162-182)

```java
@Transactional(rollbackFor = Exception.class)
public Long convertLead(LeadConvertParams params) {
    Lead lead = leadMapper.selectById(params.getLeadId());
    if (lead == null) {
        throw new BusinessException("线索不存在");
    }

    if (lead.getStatus() != null && lead.getStatus() == 3) {
        throw new BusinessException("该线索已转化");
    }

    // TODO: 创建客户记录（需要客户模块实现后补充）
    Long customerId = idGenerator.nextId();  // ❌ 问题1: 手动生成ID

    // 更新线索状态为已转化
    lead.setStatus(3);
    lead.setCustomerId(customerId);
    leadMapper.updateById(lead);

    log.info("线索转化成功，线索ID: {}, 客户ID: {}", params.getLeadId(), customerId);
    return customerId;  // ❌ 问题2: 返回的ID对应的客户根本不存在!
}
```

---

## ⚠️ 存在的问题

### 问题1: 手动ID生成
- 第173行使用 `idGenerator.nextId()` 手动生成ID
- 与MyBatis-Plus的ASSIGN_ID配置冲突

### 问题2: 客户记录未创建
- 只生成了一个customerId
- **没有实际创建Customer记录**
- 返回的customerId在数据库中不存在!

### 问题3: 前端会收到错误的ID
- 后端返回的customerId是手动生成的Long
- 经过JacksonConfig序列化为字符串
- 但这个ID对应的客户根本不存在

---

## ✅ 正确的实现逻辑

应该调用 `CustomerService.createFromLead()`:

```java
@Transactional(rollbackFor = Exception.class)
public Long convertLead(LeadConvertParams params) {
    Lead lead = leadMapper.selectById(params.getLeadId());
    if (lead == null) {
        throw new BusinessException("线索不存在");
    }

    if (lead.getStatus() != null && lead.getStatus() == 3) {
        throw new BusinessException("该线索已转化");
    }

    // ✅ 调用CustomerService创建客户
    Long customerId = customerService.createFromLead(
        params.getLeadId(),
        params.getCustomerName(),
        lead.getOwnerId()
    );

    // 更新线索状态为已转化
    lead.setStatus(3);
    lead.setCustomerId(customerId);
    leadMapper.updateById(lead);

    log.info("线索转化成功，线索ID: {}, 客户ID: {}", params.getLeadId(), customerId);
    return customerId;
}
```

---

## 🔧 需要修复的地方

### 1. LeadService.convertLead
- 删除手动ID生成
- 调用CustomerService.createFromLead创建真实客户

### 2. CustomerService.createFromLead
- 删除手动setId (L180)
- 依赖MyBatis-Plus自动生成ID

---

## 📋 修复优先级

**高优先级** - 影响业务功能:
- LeadService.convertLead - 客户未创建,业务逻辑错误

**中优先级** - ID一致性:
- CustomerService.createFromLead - 手动ID生成
