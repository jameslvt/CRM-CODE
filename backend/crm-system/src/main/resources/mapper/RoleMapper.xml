<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.system.mapper.RoleMapper">

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultType="com.crm.system.entity.Role">
        SELECT id, role_name, role_key, sort, data_scope, status, remark, create_time, update_time
        FROM crm_role
        WHERE role_key = #{roleCode} AND deleted = 0
    </select>

    <!-- 根据用户 ID 查询角色列表 -->
    <select id="selectByUserId" resultType="com.crm.system.entity.Role">
        SELECT r.id, r.role_name, r.role_key, r.sort, r.data_scope, r.status, r.remark, r.create_time, r.update_time
        FROM crm_role r
        INNER JOIN crm_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.deleted = 0 AND r.status = 1
    </select>

    <!-- 根据角色 ID 查询权限 ID 列表 -->
    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id
        FROM crm_role_permission
        WHERE role_id = #{roleId}
    </select>

    <!-- 删除角色权限关联 -->
    <delete id="deleteRolePermissionsByRoleId">
        DELETE FROM crm_role_permission WHERE role_id = #{roleId}
    </delete>

    <!-- 批量插入角色权限关联 -->
    <insert id="insertRolePermissions">
        INSERT INTO crm_role_permission (role_id, permission_id)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId})
        </foreach>
    </insert>

    <!-- 检查角色是否被用户使用 -->
    <select id="countUsersByRoleId" resultType="int">
        SELECT COUNT(*) FROM crm_user_role WHERE role_id = #{roleId}
    </select>

</mapper>
